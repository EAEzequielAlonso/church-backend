export enum FollowUpNoteType {
    INTERNAL = 'INTERNAL', // Visible to Assigned + Admins
    SHARED = 'SHARED',     // Visible to Everyone involved
    PASTORAL = 'PASTORAL', // Visible only to Admins/Auditors
}
